<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Paris in Private" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Private guided tours in Paris for American visitors. Hidden neighborhoods, long lunches, no crowds.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= yield :head %>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
